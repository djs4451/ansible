---
- name: print messages
  hosts: localhost
  gather_facts: yes
  become: true
  tasks: 
    - name: print msg
      debug:
        msg: "{{ item }}"
      with_items:
        - hello1
        - hello2 
